[build-system]
requires = [
    "setuptools==68.2.2",
    "wheel",
    "setuptools_scm[toml]>=8.0",
    "cython>=3.0",
    "numpy>=1.26",
]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
write_to = "ppmat/_version.py"
fallback_version = "0.0.0"
